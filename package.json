{
  "name": "nimbus-workers",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute main_db --local --file=./schema.sql",
    "db:prod": "wrangler d1 execute main_db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}